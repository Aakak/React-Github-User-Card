{
    "version": 1,
    "name": "github-user-card",
    "builds": [
        { "src": "app/package.json", "use": "@now/static-build ", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "github-user-card/public/$1" }
    ]
}
